import { styled } from "@mui/system";

import Box from "@mui/material/Box";

export const StyledCentredBox = styled(Box)(() => ({
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
}));
